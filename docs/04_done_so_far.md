# Что уже сделано

Дата среза: 2026-06-13.

## 1. Основной Checker / Codex / pp87-checker_new

Рабочая зона: `E:\Projects\OpenAI\pp87-checker_new`.

Сделано и обсуждено:
- сформирована идея работы через уровни сущностей: атомы, пары, блоки, корпуса, страницы, зоны;
- уточнены определения `pair`, `block`, `toc`, `body`, `corpus`;
- зафиксировано правило: `block` существует только там, где после heading сразу начинается `body_text`;
- начата база сочетаний элементов и методов;
- исследован раздел `61-23 КР лавка` как важный тестовый пример;
- подготовлены/обсуждены CSV/PQ-выгрузки для анализа методов;
- восстановлена и пересобрана база `pp87_items.json` после ошибок в исходных текстах;
- обнаружено, что `Краткие_заголовки` не могут быть безусловным источником истины, так как часть заголовков была усечена или загрязнена;
- сформирован подход к проверке заголовков ПП87 через скользящее окно и score-функции;
- получены первые сильные пики на 1480 АР по страницам 6-8;
- выявлено, что скрытые реализации, нормализации и обрезания должны быть явно документированы.

## 2. Opus / heading-template-matching

Рабочая зона: `C:\Users\alexa\Cursor\Checker87 app\pp87-checker`.

Сделано и обсуждено:
- создана ветка `heading-template-matching`;
- реализованы IDF, argmax/margin, sliding/localization, decoder, marker_fuse, batch validation;
- исследована версионность ПП87: current vs old layer;
- подтверждено, что слепой `max(old,current)` опасен из-за пере-литерации пунктов;
- сделан inventory ручной разметки: `_ввод*.json`, `page_taxonomy.txt`, `_тест.json`, `_объект.txt`, `text_layer.txt`, debug exports;
- first-block начал проверяться по ручной `page_taxonomy.txt`, а не по `app_bps`;
- текущий checkpoint из чата: first-block около 71% exact / 77% ±1 по 63 taxonomy-файлам;
- исправлены важные случаи: иерархия уровней маркера, заглавные кириллические маркеры;
- выявлен тупик max-bag/плотностного подхода к зонам: TOC-копия может перетягивать BODY;
- зафиксирована необходимость считать доменные правила жесткими ограничениями.

## 3. Fable / pdf-structure-explorer

Рабочая зона: `E:\repos\pdf-structure-explorer`.

Сделано:
- создан локальный web/backend проект для анализа структурных элементов PDF;
- реализован экспорт bundle по документу: `documents`, `pages`, `layers`, `elements`, `text_segments`, `tables`, `table_cells`, `images`, агрегаты;
- введен стабильный `document_id = doc_<crc32>`;
- обкатан корпус 75 разделов / 3362 страницы / 7.75 млн элементов;
- добавлен `page_kind`: `vector`, `hybrid`, `scanned`, `text_outlined_suspect`;
- таблицы и ячейки экспортируются с координатами и confidence;
- сделана фильтрация `symbol_grid`;
- подготовлены PowerQuery и Excel-выгрузки;
- текущие рабочие bundle находятся в `E:\output\pdf-structure-explorer\exports`;
- пример 1480 АР уже доступен как `doc_fd8c057e`.

## 4. Общие выводы

- Нужен общий слой планов, метрик, контрактов и решений.
- Личные репозитории остаются рабочими лабораториями.
- Общий слой должен хранить не код методов, а трассировку: что предложено, где реализовано, на чем проверено, какие метрики получены, какие ограничения действуют.
