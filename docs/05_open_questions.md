# Открытые вопросы

Дата среза: 2026-06-13.

## Архитектура

0. Как строится связь между physical atoms из `pdf-structure-explorer` и доменными атомами Checker, и является ли эта связь только диагностикой или основанием основного алгоритма? Базовая связь раздел -> `crc32` -> `doc_<crc32>` -> explorer bundle считается решенной через `Elements_PDF_document_links.xlsx`, лист `document_links`; открытым остается слой `physical atoms -> checker atoms`.

1. Где будет канонический источник `pp87_items` и как фиксировать версии ПП87?
2. Как формально отделять переформулировку того же пункта ПП87 от пере-литерации/другой сущности?
3. Какой минимальный набор документов должен сопровождать каждый значимый прогон?
4. Нужно ли делать общий слой отдельным git-репозиторием с коммитами решений?
5. Решена ли задача извлечения текста именно внутри СПДС-рамки, или пока доказан только физический слой PDF (`frames/text_segments/tables`) без надежного Checker-level clipping?

## Данные

1. Как инвентаризировать всю ручную разметку и связать ее с `document_id` / `file_crc32`?
2. Какие форматы ground truth считаются каноническими для first-block, corpora и item-level verdicts?
3. Как маркировать устаревшие, черновые и непроверенные разметки?

## Методы

1. Какой метод считать baseline для first-block после появления `page_taxonomy`?
2. Какие reason_code нужны для отказа candidate/corpus?
3. Как связать `pdf-structure-explorer` bundle с методами Checker без дублирования данных?
4. Как измерять качество TOC отдельно от BODY, если содержание отсутствует или является ложным?
5. ~~Выбор записи при равном маркере~~ **РЕШЕНО** (opus_checker@fedb047, 2026-06-15): среди записей с маркером E предпочитаем совпавшую с ТЕКУЩЕЙ редакцией (`is_ok_literal`), затем `argmax bag_dual`. ВС о (60-23/68-23/66-23) `wrong_text_old_edition`→`ok` (len_ratio 0.4→1.0). По конструкции монотонно. _Было: сырая строка-префикс целиком содержит короткую СТАРУЮ редакцию и выигрывает по `bag_dual`, обгоняя полную склейку._
6. Слой literalness: `stemRu` (⌈0.7·len⌉) схлопывает падеж зависимого слова → ложный `ok` на грамматической ошибке проектировщика (66-23 ТХ·ж «обеспечению выполнения→выполнению»). Нужен ли отдельный сигнал «смена падежа зависимого слова, ломающая согласование» = `wrong_text`? См. [[feedback_marker_table_cell]].

## Продуктовые замечания

1. Какой набор замечаний верхнего уровня выдавать при плохом PDF: скан, текст в кривых, битая кодировка, отсутствие поиска/копирования?
2. Как показывать пользователю доказательства замечаний: страница, bbox, текст, сопоставленный пункт ПП87, причина?

