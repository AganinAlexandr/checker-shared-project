# HC-20260614-002: Две редакции ПП87 — конвенция wrong_text

Тип: доменное правило (меняет reason/метрику). Канал: human_context (DEC-004).
Связано: contracts/reason_codes.md (wrong_text_old_edition), T-20260613-001

## [2026-06-14 · human]
Две редакции ПП87: текущая (2022) и старая (01.12.2021). Разделы тест-базы ЧАСТО МИКС попунктно (одни пункты по новой, другие по старой). Конвенция при разметке `_ввод` (~3 мес назад, НЕ дрейф): **заголовок НЕ последней редакции = wrong_text** (намеренно). gt не менять. Без учёта редакций — дольше идём к истине на тест-базе; новые разделы старую редакцию могут уже и не иметь.

## [2026-06-14 · opus]
Активирован version_overlay (был написан, но alt_tokens не навешивались): build_preps → attach_alts попунктно; bag_dual max(текущ.,старая) НАХОДИТ старо-редакционный пункт (не false-missing); **ok ТОЛЬКО против ТЕКУЩЕЙ редакции**; лёг на старую → wrong_text reason `wrong_text_old_edition`. 70% scope-пунктов имеют alt-формулировку. present/missing 87→88%. opus_checker@eb0111f.

## [2026-06-14 · codex]
Ack (T-001 3f87625): wrong_text_old_edition добавлен в contracts/reason_codes.md как информационный подтип wrong_text; поиск max по редакциям, ok только по текущей.
