# T-20260621-020: Experiment C (expert quality) — build handoff to Codex

Date: 2026-06-21
From: opus (reviewer) + human (owner)
To: codex (generator)
Spec: HC-20260621-017 (read first — full model, registry schema, filters, scale, caveats)

## Контекст

Owner-трек (RP-039..041) НА ПАУЗЕ. Живой приоритет human = **C-эксперимент: качество
эксперта** (+ задел под B = дельта-пара). opus сделал разведку (tmp-probes), Codex
**продуктизирует** в DocSpectrum-инструмент с тестами. Числа ниже — reference для воспроизведения.

opus-probes (durable, читаемы Codex): `E:/commons/DocSpectrum/experiment_c_probes_v0/`
(`tmp_expert_groups`, `tmp_gate_similarity`, `tmp_kuz_workload`, `tmp_kuz_session` .py/.txt).

## Что СТРОИТЬ СЕЙЧАС (доступно без контента замечаний)

### 1. Registry parser + candidate/gold groups
Источник: `C:\Users\alexa\OneDrive\Общая\MSE\Объекты MSE_new 2602_24.xlsx` лист «данные»,
**шапка стр.10, данные с 11** (файл бывает залочен Excel → читать копию).
Колонки (0-based): №_об=2, группа объекта=7(вид работ), подГруппа=8, проектная организация=9,
раздел=13, подРаздел=14, **вид_работы=17 (род действия ЭКСПЕРТА, не стройвид!)**, фио=20(ЭКСПЕРТ),
начало работы=27, ПОЛУЧЕНЫ замечания=28, РЕЗУЛЬТАТ_1=29, ОТВЕТ_1 ЗАКАЗЧИКА=31, Положительный=49.

**Фильтры:** drop ПОЛУЧЕНЫ замечания ∈ {01.01.2020, 01.02.2020}; drop вид_работы «отказ*»;
drop вид_работы «подп» (реальный эксперт — отдельная строка). Остаётся реальная строка.
**clean-pass** = РЕЗУЛЬТАТ_1==Положительный И ОТВЕТ_1 пуст; **had-remark** = ОТВЕТ_1 не пуст.
(Кол.28 — ТОЛЬКО для drop плейсхолдеров, НЕ сигнал.)
obj→object_id: "151525"→"1515_25"; малые zfill(4): "29224"→"0292_24".

Группа = (орг × группа объекта × раздел). **Scope раздел = КР, ПОС.** УУиР здесь актив (значение
«группа объекта», его КР/ПОС в scope). **Reference:** 207 кандидат-групп (≥2 объ, ≥2 эксперта);
**97 GOLD** (≥2 меченых эксперта С замечаниями).

### 2. Similarity gate (на ИСХОДНЫХ pre-expertise разделах)
Маппинг: `gip_control_registry_expanded_v0/gip_control_sections_v0.csv` (object_id, section_code,
bundle_id=doc_<crc>) → PSE exports `E:/output/pdf-structure-explorer/exports/doc_*`.
Профиль: composition (size-инвар. доли text/line/frame/image/table/cell из `page_summary.csv`) +
shingle (5-словные из `normalized_text` в `text_segments.csv`). **provenance-residual НЕ нужен**
(для качества эксперта дефект есть дефект → субстрат = полное содержание).
**Reference:** 57 gold-групп с ≥2 выгруженными объектами; внутри med-shingle **0.85-0.95**,
comp **~1.000** → почти-идентичность субстрата подтверждена. Гейт = порог почти-идентичности
(высокий) ИЛИ выравнивание по общему субстрату.

### 3. Arena (кросс-экспертные почти-идентичные пары)
**Reference (sh≥0.70):** Кузнецов↔класс-3 **149**, класс-1↔класс-3 **53**, **Кузнецов↔класс-1 34 пары / 6 ячеек**.
Прайм: **АО ССУ№3 / фундамент (КР+ПОС)** — Кузнецов vs Крюкова(кл.1), sh≈0.91-0.925, comp=1.0.
Метки (КР/ПОС): Золотарева=1, Крюкова=1, Левина=3, **Кузнецов=hold-out**.

### 4. Session-level variance (⚠️ ключевой методологический пункт)
**Единица наблюдения = (эксперт × СЕССИЯ/батч), НЕ объект.** Батч = одна дата «начало работы»
(+та же ячейка). Подтверждено: Кузнецов АО ССУ№3/фундамент — 24 строки переданы в 2 дня
(13-02-2025: 10 объ, все clean за неделю) = ОДНА ским-сессия, не 10 наблюдений. **Дисперсию мерить
ПОПЕРЁК сессий/дат.** Reference: clean-rate Кузнецова по месяцам 0%→100% (max дисперсия среди 4;
**month-load r=−0.15 НЕ объясняет** → давление на более мелком зерне; лучший прокси = per-section
оборот начало→РЕЗУЛЬТАТ_1). Класс-1 (Крюкова/Золотарева) низкая дисперсия; Левина(3) умеренная.

### 5. Difficulty proxy (развязка «лёгкий vs ленивый»)
Спектральная сложность DocSpectrum (число элементов/листов/структурная насыщенность из exports) —
не зависит от экспертов → нормировка покрытия на сложность.

## Что ЗАБЛОКИРОВАНО (ждёт human)
- **Контент замечаний** для recall vs потолок + типизация (по элементу/глубине). **Замечаний в
  корпусе MSE_арх НЕТ** (20/1557 файлов; источник human ЗНАЕТ, охват = выясняет). НЕ искать в корпусе.
- Приоритет survival-проверки (6 ячеек, см. HC-017): начинать с АО ССУ№3 фундамент
  (Крюкова 151325/151425 + Кузнецов 1483-1498) и СтройМонтаж СП Чердак/скатная (122124/122224).
- B (дельта-пара) — нужны post-expertise версии (тоже нет в корпусе).

## Концептуальный каркас (для интерпретации)
- **класс-1 = ПОТОЛОК шкалы** (объединение замечаний 2 класса-1 ≈ истинный набор дефектов =
  знаменатель recall). Раз класс-1 нашёл дефекты → дефекты есть → снимается «лёгкий раздел».
- Кузнецов **квалифицирован** → ось 1↔3 (не класс 2); условный/время-зависимый → **дискриминатор =
  дисперсия recall** (бимодальность), не средняя точка.
- инварианты: hash-only где персона; library-relative; информируем-не-караем (ТЗ §5.2); метки экспертов
  = ground-truth для валидации метода, не вход модели.

## Предлагаемый порядок (Codex → opus ревью)
1. Productize шаги 1-3 (registry→groups→gate→arena CSV) + тесты, воспроизвести reference-числа.
2. Шаг 4 (session-variance) + шаг 5 (difficulty proxy).
3. Стоп до контента замечаний (human) → потом recall/типизация.
