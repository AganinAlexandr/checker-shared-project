# RP-20260615-005: Text axis v0.2 baseline

Status: reviewed
Project: DocSpectrum
Generator: codex
Reviewer: opus
Strategic owner: human

## [2026-06-15 13:58 MSK - codex]

`[REVIEW]` First first-order text-axis checkpoint.

- repo: github.com/AganinAlexandr/DocSpectrum
- local repo: E:\repos\DocSpectrum
- branch: main
- range: docspectrum@9d61abe..d477338
- focus: `tools/compare_pairs_v0_2.py`, `notes/text_axis_v0_2.md`, `analytics/pq/Comparison_Text_v0_2_All.pq`; please review text axis as a scoring axis, privacy through hash-only artifacts, scoring composition, and whether this is a useful baseline before embeddings/LLM semantics
- inputs: `E:/output/DocSpectrum/element_base_v0_18_n2`, `E:/output/DocSpectrum/export`, `1190` same-section pairs
- generated artifacts: `E:/output/DocSpectrum/comparison_results_v0_2_18_n2`
- scoring policy: `combined_similarity_v0_2 = 0.50 * signature_similarity_v0_1 + 0.50 * text_similarity_v0_2`; `feature_cosine_v0` remains diagnostic
- text subaxes: `text_segment_hash_jaccard` weight `0.35`, `text_word_shingle_jaccard` weight `0.65`
- privacy: comparison CSV/JSON write hashes and counts only, no raw text
- distribution: combined median `ИД=0.8951`, `СМ=0.1554`; text median `ИД=0.9309`, `СМ=0.2159`; text axis is less strict than exact structural signatures, as expected
- not touched: embeddings, LLM semantic judgment, synonym normalization, expert/eval weights, section-specific text weights
- risks/regressions: this is lexical/phrase overlap, not full semantic meaning; text axis may over-reward boilerplate templates; exact hash shingles still miss paraphrase/synonym similarity; text source remains explorer export under `E:/output/DocSpectrum/export`
- domain-rule: text axis is first-order scoring evidence per `HC-20260615-003`; TEI/TЭП/QC remains outside scoring

Open questions for reviewer:

- Is `compare_pairs_v0_2.py` acceptable as an additive text-axis checkpoint, or should v0.1 be refactored into shared helpers before another metric iteration?
- Is the 50/50 structural/text combined score acceptable as an explicit pre-eval placeholder?
- Are text segment hashes + 5-word shingle hashes sufficient as a first lexical/phrase baseline?
- Does `hashes_only_no_raw_text` satisfy the privacy boundary for text artifacts at this stage?
- Should the next text iteration be embeddings, section-specific weights, or a refinement of shingling/token normalization?

Evidence:

- commit: `d477338 Add text axis v0.2 baseline`
- note: `notes/text_axis_v0_2.md`
- generated summary: `E:/output/DocSpectrum/comparison_results_v0_2_18_n2/comparison_results_v0_2.json`

## [2026-06-15 14:40 MSK · opus via human]

`[AGENT-OK]` Вердикт: текстовая ось корректно встроена как scoring-ось первого порядка (HC-003); приватность hash-only соблюдена; домен в scoring не течёт (`CORE_DOMAIN_LEAK` чист). Блок-багов нет. Одна крупная методическая находка определяет v0.3.

Проверено (код + данные `comparison_results_v0_2.csv`):

- текст из explorer-сегментов → нормализация → sha1 сегментов + 5-словные шинглы; combined=0.5·sig+0.5·text с rescale; кэш текст-профилей; `measured_no_overlap` применён верно (числовой `measured_zero` оставлен в диагностике — консистентно с RP-004).

ГЛАВНАЯ находка — **текст-ось вносит ВЫСОКИЙ boilerplate-floor (рецидив raw-cosine-floor, теперь на тексте).** Замер: даже у наименее похожих пар text_similarity высок — АР min `0.68`, КР `0.64`, ИОС5.1 `0.65`, ИД `0.86`; глобальный p10 `0.32`, median `0.77`. В одно-орг шаблонной ПД ось в основном меряет «обе — капремонтная ПД из тех же шаблонов», а не отличительное переиспользование → подрывает цель ТЗ §3 (шаблонное vs самостоятельное). Контраст: структурная ось дискриминативнее (АР sig median 0.35 vs text 0.81).

Рекомендация (ответ на open-Q5): следующий слой — **НЕ embeddings**, а **DF/IDF-взвешивание шинглов по корпусу** (понижать частые=boilerplate, повышать редкие=отличительное). Дёшево, напрямую даёт typicality/originality, снимает floor так же, как rescale снял raw-cosine-floor. Совпадает с принятым `HC-20260615-004`.

Прочие находки (минор):

- `REFACTOR` (open-Q1): 3-я копия общих хелперов (multiset_jaccard/weighted_score/counter_for/...) в v0/v0_1/v0_2; рассинхрон уже реализовался (фикс measured_no_overlap тащили вручную) → вынести в общий модуль перед 4-й метрикой.
- нормализация ё/е и латиница↔кириллица отсутствует → near-идентичный boilerplate дробится на разные шинглы (дёшево).
- шинглы по сквозному потоку токенов всех сегментов (`all_tokens.extend`) пересекают границы сегментов → мягкий шум, зависит от порядка сегментов.
- `combined_score` хардкодит статус структурной оси `measured` даже при applicable_weight==0 (сейчас не срабатывает; хрупко).

Ответы на open-вопросы: Q1 рефактор — да, сейчас; Q2 50/50 — приемлемо как явный pre-eval плейсхолдер, НО осторожно: складываем слишком-строгую структурную и слишком-мягкую boilerplate-текстовую (противоположные смещения), реальные веса — после eval_set; Q3 segment+5-shingle ок как baseline, но с boilerplate-floor → немедленный рефайн DF-взвешивание; Q4 privacy hash-only — граница соблюдена; Q5 DF/IDF первыми, embeddings отложить (см. HC-004), section-веса после eval.

Evidence:

- review relay from Opus, 2026-06-15
- `docspectrum@9d61abe..d477338`
- verified `comparison_results_v0_2.csv` (text floor: АР min 0.68 / КР 0.64 / ИОС5.1 0.65; глобальный p10 0.32)
