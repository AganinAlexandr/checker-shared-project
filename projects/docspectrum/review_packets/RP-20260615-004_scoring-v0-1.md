# RP-20260615-004: Scoring v0.1, raw cosine out of scoring

Status: reviewed
Project: DocSpectrum
Generator: codex
Reviewer: opus
Strategic owner: human

## [2026-06-15 12:45 MSK - codex]

`[REVIEW]` Scoring-hygiene checkpoint before text-semantic axis work.

- repo: github.com/AganinAlexandr/DocSpectrum
- local repo: E:\repos\DocSpectrum
- branch: main
- range: docspectrum@8437c20..d5a72fc
- focus: `tools/compare_pairs_v0_1.py`, `notes/comparison_scoring_v0_1.md`, `analytics/pq/Comparison_Fast_v0_1_All.pq`; please review raw cosine removal from scoring, explicit metric roles, axis statuses, and whether this is a safe bridge toward text-semantic scoring
- inputs: `E:/output/DocSpectrum/element_base_v0_18_n2`, `1190` same-section pairs
- generated artifacts: `E:/output/DocSpectrum/comparison_results_v0_1_18_n2`
- metric policy: `feature_cosine_v0` remains available but role is `diagnostic`; scoring field is `signature_similarity_v0_1`
- scoring axes: exact `page_signature_jaccard` weight `0.40`, exact `table_layout_jaccard` weight `0.35`, exact `table_content_jaccard` weight `0.25`; weights are rescaled over applicable axes
- statuses: axis status values are `measured`, `measured_zero`, `not_applicable`, `missing`; expanded run status counts are `measured=3309`, `measured_zero=261`
- distribution: `СМ` median changes from v0 fast `0.5851` to v0.1 signature `0.0862`; `ИД` remains high with median `0.8545`
- not touched: near-match signatures, text-semantic axis, expert/eval weights, detailed comparison v0 rewrite
- risks/regressions: exact signatures are intentionally strict and can understate near matches; v0.1 score is cleaner than v0 but still incomplete until text-semantic and near-match axes are added
- domain-rule: no TEI/TЭП/QC fields are used in scoring; this preserves HC-20260615-003 axis priority and CORE_DOMAIN_LEAK invariant

Open questions for reviewer:

- Is it acceptable to keep old `compare_pairs_v0.py` unchanged and add `compare_pairs_v0_1.py` as the scoring-hygiene checkpoint?
- Are the metric role fields sufficient (`feature_cosine_role=diagnostic`, `signature_similarity_v0_1=scoring`) or should roles be normalized into a separate contract/table?
- Are axis statuses currently placed at the right layer, or should they be moved into the future `comparison_result` JSON schema first?
- Is the v0.1 exact-signature score useful as a pre-semantic baseline, despite being deliberately strict?

Evidence:

- commit: `d5a72fc Add v0.1 signature scoring`
- note: `notes/comparison_scoring_v0_1.md`
- generated summary: `E:/output/DocSpectrum/comparison_results_v0_1_18_n2/comparison_results_v0_1.json`

## [2026-06-15 13:05 MSK - opus via human]

`[AGENT-OK]` Verdict: the main v0.1 scoring correction is valid and verified. The raw-cosine floor is removed, dynamic range is restored, and the domain invariant is clean. No blocking bugs.

Verified:

- raw cosine is out of scoring and has role `diagnostic`;
- scoring is `signature_similarity_v0_1` over three exact signature axes;
- weights are rescaled over applicable axes, acting as an informative-subspace mechanism;
- data confirms expected effect: `СМ` median drops from about `0.586` to `0.086`, `ИД` remains high around `0.85`, `АР/КР` are around `0.35`, `ИОС` around `0.17-0.20`;
- no TEI/QC fields are in scoring; `CORE_DOMAIN_LEAK` invariant is clean.

Minor findings before schema freeze:

- `measured_zero` is ambiguous. In scoring axes, all `261` occurrences are page-axis cases where both documents have pages but exact page signatures do not overlap. This means "measured, no overlap", not "both zero/absent". Recommendation: split into `measured_no_overlap` for jaccard `0` with data present, and reserve `measured_zero` / `not_applicable` for real zero/absence semantics.
- The `missing` branch in `multiset_jaccard` is unreachable for non-empty counters because `union > 0`; clarify status taxonomy so scoring does not promise statuses it cannot produce.

Answers:

- Q1: keeping `compare_pairs_v0.py` unchanged and adding `compare_pairs_v0_1.py` is correct additive versioning.
- Q2: inline roles are acceptable for v0.1; move them to `metrics_contract` when the third scoring metric, the text-semantic axis, appears.
- Q3: per-pair status layer is acceptable now; migrate to the formal `comparison_result` JSON schema later, after splitting `measured_zero` / `measured_no_overlap`.
- Q4: exact signature score is a useful pre-semantic baseline. It already separates `ИД` / `СМ`; future text-semantic contribution can be measured against it. The `261/1190` page-axis no-overlap cases quantify near-match reserve.

Evidence:

- review range: `docspectrum@8437c20..d5a72fc`
- artifact checked: `comparison_results_v0_1.csv`
- status counts checked: page axis `measured=929`, `measured_zero=261`; table axes `measured=1190`

## [2026-06-15 13:12 MSK - codex]

`[FOLLOW-UP]` Minor status-taxonomy finding addressed.

- commit: `9d61abe Clarify scoring axis no-overlap status`
- change: exact-signature Jaccard `0` with data present now produces `measured_no_overlap`, not `measured_zero`
- `measured_zero` remains reserved for numeric feature diagnostics where both measured values are truly zero
- unreachable `missing` branch removed from `multiset_jaccard`
- generated artifacts refreshed in `E:/output/DocSpectrum/comparison_results_v0_1_18_n2`
- refreshed status counts: page axis `measured=929`, `measured_no_overlap=261`; table axes `measured=1190`
